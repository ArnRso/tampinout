{% extends 'base.html.twig' %}

{% block title %}Mon suivi - tampinout{% endblock %}

{% block body %}
<div class="min-h-[calc(100vh-4rem)] flex items-center justify-center bg-base-200 py-8">
    <div class="card w-full max-w-md bg-base-100 shadow-xl">
        <div class="card-body items-center text-center">
            <h1 class="text-3xl font-bold mb-6">Mon suivi</h1>

            {% if hasActiveTampon %}
                <div class="text-8xl mb-4">🩸</div>
                <p class="text-xl mb-2 font-semibold">Tampon en place</p>
                {% if lastEvent %}
                    <p class="text-sm text-base-content/70 mb-2">
                        Inséré le {{ lastEvent.createdAt|date('d/m/Y') }} à {{ lastEvent.createdAt|date('H:i') }}
                    </p>
                    <p class="text-sm text-base-content/70 mb-8">N'oubliez pas de le changer régulièrement</p>
                {% endif %}
            {% else %}
                <div class="text-8xl mb-4">✅</div>
                <p class="text-xl mb-2 font-semibold">Pas de tampon</p>
                <p class="text-sm text-base-content/70 mb-8">Tout va bien !</p>
            {% endif %}

            <form method="post" action="{{ path('app_tampon_toggle') }}" onsubmit="return confirm('Confirmer : {{ hasActiveTampon ? 'retirer le tampon' : 'insérer un tampon' }} ?');">
                <input type="hidden" name="_token" value="{{ csrf_token('tampon_toggle') }}">
                <button type="submit" class="btn btn-primary btn-lg">
                    {% if hasActiveTampon %}
                        J'ai retiré mon tampon
                    {% else %}
                        J'ai inséré un tampon
                    {% endif %}
                </button>
            </form>
        </div>
    </div>
</div>
{% endblock %}
