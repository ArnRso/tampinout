{% extends 'base.html.twig' %}

{% block title %}tampinout - Ne perdez plus le fil{% endblock %}

{% block body %}
{% if app.user %}
    {# Page pour utilisateur connecté - à venir #}
    <div class="min-h-[calc(100vh-4rem)] flex items-center justify-center bg-base-200">
        <div class="text-center">
            <h1 class="text-4xl font-bold mb-4">Bienvenue {{ app.user.firstName }} !</h1>
            <p class="text-lg">Votre tableau de bord arrive bientôt...</p>
        </div>
    </div>
{% else %}
    {# Page d'accueil pour visiteurs non connectés #}
    <div class="min-h-[calc(100vh-4rem)] bg-gradient-to-b from-primary/10 to-base-200">
        {# Hero Section #}
        <div class="hero min-h-[60vh]">
            <div class="hero-content text-center">
                <div class="max-w-2xl">
                    <h1 class="text-5xl font-bold mb-6">
                        Ne perdez plus le fil de vos règles
                    </h1>
                    <p class="text-xl mb-8">
                        tampinout vous aide à suivre l'utilisation de vos tampons en temps réel.
                        Un simple clic pour savoir si vous avez un tampon en ce moment.
                    </p>
                    <div class="flex gap-4 justify-center">
                        <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg">
                            Commencer gratuitement
                        </a>
                        <a href="{{ path('app_login') }}" class="btn btn-outline btn-lg">
                            Se connecter
                        </a>
                    </div>
                </div>
            </div>
        </div>

        {# Features Section #}
        <div class="container mx-auto px-4 py-16">
            <h2 class="text-3xl font-bold text-center mb-12">Pourquoi tampinout ?</h2>

            <div class="grid md:grid-cols-3 gap-8">
                {# Feature 1 #}
                <div class="card bg-base-100 shadow-xl">
                    <div class="card-body items-center text-center">
                        <div class="text-5xl mb-4">🩸</div>
                        <h3 class="card-title">Suivi simple</h3>
                        <p>Un clic suffit pour enregistrer la pose ou le retrait de votre tampon. Plus de doute, plus d'inquiétude.</p>
                    </div>
                </div>

                {# Feature 2 #}
                <div class="card bg-base-100 shadow-xl">
                    <div class="card-body items-center text-center">
                        <div class="text-5xl mb-4">⏰</div>
                        <h3 class="card-title">Rappels intelligents</h3>
                        <p>Ne dépassez jamais le temps recommandé. Recevez des notifications pour changer votre tampon à temps.</p>
                    </div>
                </div>

                {# Feature 3 #}
                <div class="card bg-base-100 shadow-xl">
                    <div class="card-body items-center text-center">
                        <div class="text-5xl mb-4">🔒</div>
                        <h3 class="card-title">Données privées</h3>
                        <p>Vos informations restent confidentielles et sécurisées. Vous seule avez accès à vos données.</p>
                    </div>
                </div>
            </div>
        </div>

        {# CTA Section #}
        <div class="bg-primary text-primary-content py-16">
            <div class="container mx-auto px-4 text-center">
                <h2 class="text-3xl font-bold mb-4">Prête à prendre le contrôle ?</h2>
                <p class="text-lg mb-8">Rejoignez les utilisatrices qui ont choisi la tranquillité d'esprit.</p>
                <a href="{{ path('app_register') }}" class="btn btn-secondary btn-lg">
                    Créer mon compte gratuitement
                </a>
            </div>
        </div>
    </div>
{% endif %}
{% endblock %}
